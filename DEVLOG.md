# TEX-RAY 工作系統｜開發流水帳

> 這份記錄「改了什麼、為什麼那樣決定、踩到什麼坑」。
> 決策的最終狀態看 `工作系統_骨幹規劃_v1.md`，這裡記過程。
> 新對話先讀骨幹規劃，再從 GitHub 讀線上最新的這份與 `index.html`——不要憑記憶或知識庫舊版改。

---

## 2026-08-04　殼上線（`app.html`）

### 做了什麼

把 `sp.html` 從「一個檔案一個工具」改成「一個殼 ＋ 多個模組」。

- 左側九個選單項目，分四組（工作／專案／資料／系統）＋ 儀表板
- 只有儀表板與展覽是活的；其他七個點進去顯示「尚未建置」與該模組的規劃說明
- 窄螢幕（820px 以下）選單收成漢堡抽屜 ＋ 半透明遮罩
- 新增設定區 `LISTS`：所有清單 ID 集中一處，新模組只要填 ID
- 新增共用函式 `spGet` / `spPatch` / `spCreate` / `spSoftDelete` / `toast`
- 新增儀表板模組：下一場展覽、前置階段未完成的場次、展覽統計
- 展覽詳情頁新增「前置階段」區塊，三個階段可直接點按切換

`sp.html` 保留不動當退路，載入失敗時畫面會顯示它的連結。

### 為什麼這樣決定

**改殼排在第二個模組之前，不等使用回饋。**
使用回饋會是關於欄位和手機排版，不會改變選單結構；但同事的第一印象只有一次。
兩個檔案合成殼還容易，三個以上開始痛。

**權限先試可寫、失敗自動退回唯讀。**
原本計畫直接把 `SCOPES` 改成 `Sites.ReadWrite.All`，但那樣在組織同意之前
連讀取都會失敗，等於上傳新版後網頁打不開。
改成 `getToken()` 先要 `Sites.ReadWrite.All`（這會觸發同意視窗），
失敗就退回 `Sites.Read.All`。核准前唯讀可用，核准後寫入自動生效，不用改程式。

第一次開啟可能出現兩個彈出視窗（可寫入的同意畫面失敗後，再跳唯讀登入）。
這是可接受的代價，換來的是不會開不了。

**寫入函式放在殼裡，只寫一次。**
不讓每個模組各自寫 PATCH。跟 `FIELD_MAP` 同一個道理：
最麻煩的部分做成一次性的，第 N 個模組才會便宜。

**展覽模組的 DOM id 完全沿用 `sp.html`。**
`view-main`、`view-detail`、`stat-total`、`d-name` 等等一個都沒改，
所以繪製邏輯原樣搬過來即可，不必重寫、不會改變既有行為。

**畫面上顯示「唯讀／可寫入」標籤。**
使用者需要知道現在能不能改東西，否則點了沒反應會以為壞掉。

### 寫入慣例（本次定案，全系統適用）

判斷方法全系統一致，套用後的結果每個欄位不同。

1. **可逆的單一欄位 → 點了就存，沒有按鈕。**
2. **一組有推導關係的欄位 → 一顆按鈕，一律叫「儲存」。**
   「剛好排在同一張卡」不算一組（卯時修正過一次的判準）。
3. **不可逆或會通知別人的 → 一定要按，用動詞命名**（「結案」「指派」「登錄新版本」）。
4. **刪除一律軟刪除**，改 `IsDeleted` 欄位，各模組過濾掉它。

本次已套用：展覽的報名／繳費／選位屬規則 1，點了就存。

已知的例外要記在這裡（目前無）。

### 這次的坑

**規則 2 有一個容易誤判的地方。** 對外資料的「登錄新版本」（換連結＋換有效期）
看起來像兩個獨立欄位，但它們有推導關係，而且會讓那一行所有出口翻成「待更新」。
有連鎖效果的動作不能點了就存，否則會不小心清掉一整排勾。歸規則 3。

**MSAL 的 UMD 檔在 `lib/` 不在 `dist/`。** 沿用 `sp.html` 已驗證的
`cdn.jsdelivr.net/npm/@azure/msal-browser@2.39.0/lib/msal-browser.min.js`，沒有改。

---

## 2026-08-04（第二次）　手機登入修正

### 問題

手機開啟 `app.html` 出現 `popup_window_error: window.open returned null`。
原因：從 LINE 內建瀏覽器開啟，那類瀏覽器一律封鎖 `window.open`；iOS Safari 也常擋
非使用者點擊觸發的彈出視窗。

**這不是測試意外，是真實情境。** 網址一定會用 LINE 或 Teams 傳給同事，
他們點開就是內建瀏覽器。

排版本身沒問題：抽屉選單、漢堡、兩欄統計卡、「尚未建置」頁在手機上都正常，未修改。

### 改了什麼

**一、登入加入整頁轉向的退路。**
`needsRedirect()` 判斷內建瀏覽器（LINE／Teams／FB／IG／WebView）或 820px 以下寬度，
那些情況直接走 `loginRedirect`；桌機仍走彈出視窗，若被擋（`isPopupBlocked`）自動改轉向。
啟動時先 `handleRedirectPromise()` 接住轉向回來的回應。

**二、寫入權限改成靜默偵測。**
原本每次載入都嘗試互動式取得 `Sites.ReadWrite.All`、失敗、再登入一次唯讀——
在手機轉向流程下會變成連續兩次跳頁，很難看。
改成 `probeWrite()` 用 `acquireTokenSilent` 偷偷試：
成功則 `canWrite = true`，失敗就維持唯讀，不彈任何視窗。
**IT 核准後不需要改程式、不需要重新授權，重新整理就會自己變「可寫入」。**

**三、載入失敗時隱藏所有模組，加上「重新載入」按鈕。**
原本失敗後點側邊選單會看到空的模組（統計卡全是「-」），容易誤判為壞掉。

**四、儀表板移除「本年度展覽數」。**
使用者指出暫不參加不該列入計算。展覽模組內部維持顯示全年總數（點進去才看到）。

### 需要在 Entra 加一個網址（使用者自己就能做，不需管理員同意）

整頁轉向登入完成後，瀏覽器會跳回「重新導向 URI」。
目前 Entra 只註冊了根目錄 `https://yulunyeh0731.github.io/`，
那會跳到舊的 `index.html`（讀 Google Sheet 的退路版），拿不到權杖。

**必須加上 `https://yulunyeh0731.github.io/app.html`。**

刻意的取捨：`MSAL_CONFIG` 的 `redirectUri` 保持根目錄不動，
只在轉向的呼叫裡指定 `REDIRECT_URI_APP`。
這樣桌機的彈出視窗流程完全不變、今天就能用；
只有手機轉向需要新網址。若配置改成 app.html，桌機會在註冊完成前壞掉。

### 本次確立的判斷（避免下個對話重新討論）

- **儀表板只顯示需要採取行動的項目。** 已完成、已結案、已放棄一律排除。
  （待確認：「已結案」按這條也該移除，使用者尚未回覆。）
- **不換資料庫。** SharePoint 清單換成 Postgres／Supabase 會失去登入、權限、
  稽核、以及「資料在公司自有基礎設施」四樣。以現有資料量（展覽 13 筆、
  認證約三十筆），5,000 筆檢視門檻與無 JOIN 的限制數年內碰不到。
- **Claude Code 現在不需要，但不要拖太久。** 判斷點不是檔案大小，是
  「Claude 能不能一次把整份檔案重寫給使用者」。走到只能給「第幾行改成什麼」
  就太晚了——使用者不寫程式，改錯括號整頁白掉且無法自行排查。
  預估在第三到第四個模組。
- **Claude Cowork 不適合做這套系統**（地端個人工具無法多人共用），
  但很適合一次性的資料整理：Excel 轉匯入格式、舊資料清理、欄位設計盤點。
- **Vercel 值得做，理由是預覽環境**（每次改動先有測試網址），
  那是目前缺的安全網。但要等公司 GitHub 組織的答案再動，
  否則程式碼仍在個人 repo，治理問題只解一半。
  另需確認付費方案——Hobby 條款為非商業用途。

## 2026-08-04（第三次）　殼升為正式版，舊版封存

### 推翻一條既有定案

`展覽管理系統_交接說明_v2.md` 記載「`index.html` — 舊版，仍讀 Google Sheet，
刻意保留當退路，不要改它」。**本次推翻它。**

使用者指出：一直繞開 `index.html` 是在遷就舊版，而現在做的就是正式版。這個判斷正確。

推翻的理由：

**一、當初的條件已經消失。** 那條定案訂於 `sp.html` 剛完成、尚未驗證時。
現在 `sp.html` 已運作、`app.html` 也已證明能讀到 13 筆資料。

**二、GitHub 本身就是退路。** 每次上傳都是一個 commit，任何舊版隨時可取回。
留一個檔案當退路是與版本控制重複，而且製造「哪一個才是正式版」的混淆。
（使用者是 GitHub 新手，先前未意識到已有這個安全網。）

**三、根目錄本來就註冊在 Entra。** 殼放進 `index.html` 後，
整頁轉向登入使用 `https://yulunyeh0731.github.io/`，
**不需要新增任何重新導向 URI**——上一則要求的 Entra 設定因此取消。

**四、Google Sheet 版本本身有害。** DEVLOG 已標記「兩份資料會分歧」，
資料源已全數移至 SharePoint。

### 檔案結構變更

| 舊 | 新 |
|---|---|
| `app.html`（殼） | `index.html`（正式版） |
| `index.html`（Google Sheet 版） | `archive/index-googlesheet.html` |
| `sp.html` | `archive/sp.html` |

封存檔仍可直接以網址開啟（GitHub Pages 服務任何路徑），
所以退路實際上沒有消失，只是根目錄變得沒有歧義。
`sp.html` 用 `loginPopup` 且 redirectUri 為根目錄，搬到子路徑後仍可運作。

### 程式變更

- 新增單一 `REDIRECT_URI` 常數，`MSAL_CONFIG` 與轉向呼叫共用，只有一處要改
- 移除 `REDIRECT_URI_APP`
- 載入失敗的退路連結改指 `./archive/sp.html`

### 這個改動帶出的一個缺口

正式版現在就是根目錄，所以**上傳壞掉的版本等於所有人立刻看到壞掉的頁面**。
目前沒有人拿到網址，所以還不痛；一旦發給同事就會痛。

這正是 Vercel 預覽環境要解決的事——每次改動先產生測試網址，確認後才推正式站。
**因此 Vercel 的優先度提高，但仍應等公司 GitHub 組織的答案。**
在那之前的替代做法：改動先在本機用瀏覽器開檔案測過再上傳。

## 2026-08-05　登入改為使用者點按觸發

### 問題

手機從 LINE 內建瀏覽器開啟，整頁轉向登入失敗：
`AADSTS900561: The endpoint only accepts POST requests. Received a GET request.`

**成因尚未確認**，兩個可能，需實測分辨：

1. LINE 內建瀏覽器對轉向回應的處理有問題 → 用 Safari 直接開會成功
2. Entra 裡 `https://yulunyeh0731.github.io/` 被登記在 **Web** 平台區而非
   **單頁應用程式（SPA）** 區 → Safari 也會一樣錯

交接文件記載平台為 SPA，但一個應用程式可同時有兩區，仍須確認該網址落在哪一區。

### 一併修正的設計問題

發現一個更根本的錯誤：**原本是頁面一載入就自動要求登入。**
瀏覽器封鎖彈出視窗的判準正是「不是使用者點擊觸發的就擋」——
所以第一次的 `popup_window_error` 其實有一部分是我們自己造成的。

改為：

- 未登入時顯示一張登入卡，上面一顆「使用公司帳號登入」按鈕
- **點按**才呼叫 `loginPopup`，這樣多數手機瀏覽器會放行
- 只有在偵測到彈出視窗被擋（`isPopupBlocked`）時才退回整頁轉向
- 移除 `needsRedirect()`（原本依螢幕寬度強制轉向），改為 `isInAppBrowser()`，
  只用來決定要不要顯示「請改用 Safari 開啟」的提示
- `getToken()` 的續期同樣改為先試彈出視窗、失敗才轉向

在內建瀏覽器裡會多顯示一行提示，告知可從選單選「用 Safari 開啟」。
這是內建瀏覽器的標準脫離方式，值得直接寫在畫面上讓同事自己解決。

### 結構調整

`boot()` 拆成三個函式，職責分開：

- `boot()` — 初始化、接住轉向回應、判斷是否已登入
- `signIn()` — 使用者點按後才執行，處理彈出視窗與轉向退路
- `loadData()` — 偵測寫入權限、讀清單、繪製

好處是登入失敗不會再連帶顯示空的統計卡，而且轉向回來後可直接進 `loadData()`。

## 2026-08-05（第二次）　移除彈出視窗，登入只走整頁轉向

### 線上實測發現網站是壞的

用瀏覽器實際開 `https://yulunyeh0731.github.io/`：
`載入失敗：user_cancelled: User cancelled the flow.`

檢查頁面確認線上版本是「手機登入修正」那一版（有 `probeWrite`、沒有登入按鈕），
也就是**上一次的登入按鈕修正尚未上傳**。

### 真正的根因

殼升為 `index.html` 之後，重新導向 URI（根目錄）指向的就是整套系統本身。
`loginPopup` 完成後彈出視窗會跳回根目錄 → **整套系統在彈出視窗裡又跑一次** →
母視窗與彈出視窗搶同一份授權碼 → `block_nested_popups` 或 `user_cancelled`。

以前不會發生，是因為根目錄是舊的 Google Sheet 版、沒有 MSAL，
彈出視窗跳回去只是載入一個什麼都不做的頁面。**那是碰巧成立的。**

**上一次的登入按鈕版本也修不好這個**——它仍然先試彈出視窗，一樣會跳回根目錄。
當時只考慮了手機，沒考慮桌機的巢狀彈窗。

### 改了什麼

**彈出視窗完全移除，登入只走整頁轉向。**

- `signIn()` 只呼叫 `loginRedirect`
- `getToken()` 續期失敗時只呼叫 `acquireTokenRedirect`
- 移除 `isPopupBlocked()` 與所有 `loginPopup` / `acquireTokenPopup`
- 保留登入按鈕（使用者點按才跳轉，避免自動轉向造成迴圈）
- 保留內建瀏覽器提示，文字改為「若登入卡住」

**為什麼選這條而不是另外做一個空白的 auth.html 當彈窗跳板：**
一條路徑就不會再有這一類 bug。這幾次的錯誤全部出在「兩條路、看情況選一條」的設計上。
代價是登入時整頁跳走再跳回來，比彈出視窗少一點流暢，但可靠得多。
而且手機 Safari 那次成功走的本來就是轉向，已經驗證過。

### 這次的坑（值得記住的通則）

**改變一個檔案的角色，會改變所有指向它的東西的語意。**
把殼升成 `index.html` 是對的決定，但根目錄同時也是 OAuth 的重新導向 URI——
那個身分本來由一個「什麼都不做的頁面」承擔。升級之後它變成整套系統，
於是原本無害的往返變成了自我遞迴。這類副作用不會在程式碼裡顯示，
只會在實際跑的時候出現，所以**每次改動託管結構都要真的開一次網頁**。

### 同時做的 Entra 設定

已在「展覽管理儀表板」的 API 權限加入 `Sites.ReadWrite.All`（委派），
狀態為空白＝等待管理員同意。`Sites.Read.All` 的既有同意未受影響。

Entra 顯示警告：「您正在編輯應用程式的權限，所以即使使用者之前已同意過，
仍然必須再次同意。」因此核准前可能會在登入時跳出同意畫面。

## 2026-08-05（第三次）　更名為「工作台」，並修正一項理解錯誤

### 改名

`TEX-RAY 工作系統` → 主標「**工作台**」＋ 副標「**Yulun ・ 企劃與總務**」。

使用者指出原名不合適：這套東西是個人與小組協作，加上一點對外唯讀的內容，
「TEX-RAY 工作系統」聽起來像公司級系統。**那個名字會招來錯誤的期待**——
其他部門會覺得自己也該被服務，而她只有 7～8 人。

「工作台」是個人尺度的比喻，不綁部門、職務變動也不用改。
主標不變、副標可變，所以分兩行——與「按變動速度分層」一致。

改動三處：`<title>`、側邊欄品牌（新增 `.brand-name` / `.brand-sub`）、登入卡。
登入卡順便移除「請使用公司帳號登入」那行，因為按鈕上已經寫了。

GitHub 的 README 與 About 仍寫「TEX-RAY 展覽管理系統」，待一併更新。

### 修正：把一份參考資料當成規格

Claude 把一份前任課長的交接清單當成使用者本人的工作清單，一路當前提用了很多輪，
直到她指出來才發現。年度行事曆模組原本要照那份清單的九項來設計——**那樣會做錯。**

**使用者的糾正有兩層，第二層更重要：**

第一層是事實：那不是她的工作清單。
第二層是她接著說的——**「你甚至不需要一直提到他，為他寫什麼 Instructions，
他就是其中一份參考資料而已，會有很多很多參考資料。」**

Claude 的第一次修正是為那份檔案在骨幹規劃和 Instructions 各寫一條規則。
**那是錯的處理方式**：加規則修不了判斷錯誤，只會讓 Instructions 越來越長，
而且反而放大了那份資料的份量。已全部改為不點名的通則。

### 這次的教訓

**上游的事實錯了，下游所有設計都會錯，而且不會自己冒出來**——
因為它從來沒有被直接討論，只是被當成前提一路用下去。

**參考資料是背景，不是規格。** 使用者手上會有很多參考資料，
收到任何一份都要先確認「這是誰的、現在還有效、她實際上還做不做」。

**判斷錯誤要用判斷修正，不要用加規則修正。**

### 年度行事曆的內容改為待確認

骨幹規劃原本列出九項年度工作，那是從那份交接清單推的，已移除。
改為記錄「會落在這裡的類型」，實際內容要從使用者實際在追的工作問出來。

---

## 2026-08-07　配色改為 TEX-RAY 品牌色系，語意四色改為兩階架構

### 做了什麼

**只動 `<style>` 區塊。`</style>` 之後的 HTML 與 JS 逐位元組未改。**
944 行 → 957 行，89 行差異，全部在 CSS。

- 中性色改暖底：`--bg` `#FAFAFA` → `#F7F6F2`、`--border` `#EAEAEA` → `#E8E4DE`、
  `--text` `#18181B` → `#233F4A`（深海藍壓深）、次要字與小標改暖褐
- 新增 `--ink: #2E5665`（官方深海藍）當骨架色。選中的選單項、年度分頁、
  詳情分頁底線，全部從純黑 `var(--text)` 改成它
- 新增官方品牌色常數 `--brand-deep` / `--brand-amber` / `--brand-slate`
- 語意四色整組重畫，改成**兩階架構**（見下）
- 拆開灰色：新增 `--surface` `#F3F1EC` 與 `--surface-hover` `#E8E5DE`，
  刪除 `--gray-bg`。原本 `--gray-bg` 的五個「純底色」用途改指 `--surface`
- 四個標籤（`.tag-*`）文字統一改成 `--label: #4A4133`，不再各自帶語意色
- `.mode-ro` `.mode-rw` `.stage-on` `.stage-off` 同樣改用 `--label`
- 統計卡加 3px 左側記號線（`border-left-width`，沿用既有的 `border` 宣告，沒有改版面），
  數字改中性色
- `.ex-row.c-gray` 的左側線從 `--gray-soft` 改成 `--gray`（淡底當線看不見）
- 側邊欄品牌名前加一顆 7px 暖陽黃方塊，用 `.brand-name::before`，沒有改 HTML

新舊語意色對照：

| | 舊主色 | 新記號色（線／點） | 新淡底 | 小字色 |
|---|---|---|---|---|
| 進行中 | `#1D4ED8` | `#3D74C9` | `#F4F7FC` | `--label` |
| 待決定 | `#B45309` | `#C78803` | `#FEF7E7` | `--label` |
| 已完成 | `#15803D` | `#3E9B70` | `#F1F6F3` | `--label` |
| 不適用 | `#6B7280` | `#97938A` | `#F3F3F1` | `--label` |

對比度全部通過：正文 9.89～11.16:1、次要字 5.15～5.81、小標 4.51～5.09、
標籤文字 9.02～9.39、記號線 3.02～4.63（非文字圖形門檻 3:1）、
白字壓深海藍 7.97。

### 為什麼這樣決定

**純黑突兀，是因為它不屬於任何色系，不是因為它太深。**
`#18181B` 的彩度只有 6%——底色一旦帶色調，那塊黑就是全頁唯一無色系歸屬的東西。
換成深海藍之後，深度沒變，刺眼感消失。所以沒有把它變淺。

**兩階架構是這次最重要的結構決定：一個色值不可能同時當文字和漂亮的色塊。**
暖陽黃 `#FFAD00` 當記號漂亮，但當文字壓白底只有 1.87:1；壓暗到 4.5:1 就變成
`#906305` 那種橄欖色，當記號就不好看了。舊設計是一個值兼三個工作
（標籤文字色＋左側線色＋統計數字色），所以永遠在妥協。
這解釋了為什麼舊的橘黃在自己的淡底上只剩 +0.22 餘裕、綠 +0.23、灰 −0.07：
那不是選色不小心，是架構問題。

拆開之後：記號階只管好看、文字階只管看得清、小字一律用 `--label`。
每一邊都不再被另一邊拖住。

**標籤文字改成統一的暖中性深色，是為了避免深黃。**
黃色只有亮的時候好看，壓暗必然變橄欖。所以「待決定」的文字階乾脆不存在——
顏色由淡底和左側線承載。為了四個標籤處理方式一致（不一致的那一個一定會跳），
另外三個也一起改。

**純暖陽黃只出現在側邊欄品牌方塊，一處。**
那個位置永遠在、永遠不變，不編碼任何狀態，所以不可能被讀成「待決定」。
語意用的是它壓深到 3:1 的 `#C78803`。

**青石藍 `#506D85` 沒有用。**
它是官方品牌色，最自然的想法是拿它當「進行中」，但它跟骨架用的深海藍
ΔE 只有 0.093——側邊欄選中項和展覽列的線會糊在一起。
改用獨立的 `#3D74C9`（跟深海藍 ΔE 0.171，將近兩倍距離）。
理由就是這個距離，沒有別的。

**底色調是暖的，因為使用者選它。**
識別手冊在這個專案裡只有一個用途：提供三個公司標準色的正確色值。
這一頁的配色由使用者決定，不受手冊規範——這點使用者講過三次，
記在這裡是為了讓下一個對話不要再把它當成合規問題重新討論。

### 這次的坑

**把官方品牌色當成裝飾丟掉了。**
在拿到識別手冊之前，我從一張簡報 PNG 取樣，量到 `#4C6A82` 這個中藍灰，
然後明確判斷「那是這一份模板的裝飾選擇，logo 才是品牌本身」。
它其實是官方品牌色**青石藍 Pantone 2167C**。
教訓：從樣本反推「哪些是品牌、哪些是裝飾」這件事本身就不可靠，
沒有規範時應該說「無法判斷」，不是給一個聽起來合理的分類。

**PNG 取樣的誤差比預期小，但方向一致。**
取樣值 vs 官方：`#204B5B` vs `#2E5665`、`#F5A601` vs `#FFAD00`、
`#4C6A82` vs `#506D85`。ΔE 0.041 / 0.025 / 0.011，色相差 0～1°，
全部偏暗（PNG 壓縮）。中性色是從色相 225° 推的，官方是 224°，**差 1°，不用重推。**
但我第一時間說「取樣的色值是錯的」，那句講重了，數字不支持。

**感知均勻不等於好看。**
把四色的 OKLCH 彩度統一到同一個數字（0.105），產出的是
`#726F19` 的橄欖和 `#6766A7` 的濁紫。黃色被迫壓暗會變橄欖、
藍色彩度不夠會偏紫。均勻是起點，不是答案。最後改成
**主色的色相與彩度手挑（那是長相），亮度與淡階由數學解（那是達標）。**

**OKLCH 反算超出 sRGB 色域時會扭掉色相。**
`#35548C` 的色相 261° 被夾成 296°（藍變紫），因為我只做 RGB 通道夾限。
改成「往白色調和」產生淡階，色相絕對不會跑掉。笨方法，但可靠。

**用 HSL 判斷色相差，兩次得到錯的結論。**
先說品牌黃跟語意橘黃「色相只差 15°，是紅線」，用 OKLCH 重量是 26°，
而且感知亮度差 23 點——不是紅線。HSL 在色相上不可靠，色相判斷一律用 OKLCH。

**做過但被使用者篩掉的：色盲模擬。**
我跑了紅綠色盲模擬，發現舊四色的「待決定」和「已完成」會塌成同一個橄欖
（ΔE 0.220 → 0.069），並拿它當設計驅動因素，還據此提議把「不適用」的顏色拿掉。
使用者指出她要的功能性是「一般慣性直覺」，不是無障礙工程。
提議已收回（灰＝不適用是最強的一般直覺）。模擬結果記在下面待確認，不推設計。

**另一個被夾帶的前提：以為四色慣例是同事的既有習慣。**
我用「綠＝完成是唯一不用教就會的，改了其他部門會問」來反對大改。
使用者指出那個慣例是上一輪 Claude 對話生出來的建議，同事根本沒有慣例。
沒確認就當前提用——跟之前把交接清單當工作清單是同一個毛病。

## 待處理

**優先（擋住「發給同事」）**
- [ ] 依「檔案結構變更」搬檔：舊 index 與 sp 移入 `archive/`，殼上傳為 `index.html`，刪除 `app.html`
- [ ] 上傳 `DEVLOG.md` 與骨幹規劃
- [ ] **用 Safari 直接開根目錄網址測一次**（分辨 AADSTS900561 的成因）
- [ ] 檢查 Entra →「驗證」：根目錄網址是列在 SPA 區還是 Web 區
- [ ] 手機重測，確認登入按鈕可用、能讀到 13 筆
- [ ] 等 IT 核准 `Sites.ReadWrite.All`（已在 Entra 加好，等「代表組織授與管理員同意」）
- [ ] 上傳後實際開一次根目錄確認登入成功——不要只看程式碼

**不用等，隨時可做**
- [ ] 設網站權限：「所有人（除了外部使用者）」加進「內部工具」網站 Visitors
- [ ] GitHub 帳號開啟兩階段驗證
- [ ] 上傳時把 commit 訊息改成一句說明（開發歷程從這些訊息長出來）

- [ ] 更新 GitHub 的 README.md 與 repo About（仍寫「TEX-RAY 展覽管理系統」）
- [ ] 建年度行事曆前，先問出使用者實際每年／每半年要交出去的東西有哪些

**待決定（配色相關，這次刻意沒做）**
- [ ] 舊的小標籤對比度偏低問題已隨這次改動一併解決（統一用 `--label`，9:1 以上）
- [ ] `--red: #B91C1C`（錯誤浮層）沒有跟著校進新色系。它只出現在 toast，
      不會跟其他語意色並排，暫時不動
- [ ] 想讓介面更有 TEX-RAY 的味道的話，可以換字體（公司對外用思源黑體＋Lato，
      思源黑體＝Noto Sans TC，Google Fonts 有）。純粹是選項，不是該補的缺口
- [ ] 紅綠色盲下「待決定」與「已完成」的可辨識距離：舊版 0.069（危險）。
      新版記號色是 `#C78803` 與 `#3E9B70`，未重測。使用者已明確表示
      這不是她的判準，**不要主動拿它推設計**，除非她自己提

**待決定**
- [ ] 儀表板的「已結案」要不要也移除（按「只顯示需要行動的項目」該移）
- [ ] 公司 GitHub 組織是否成真 → 決定要不要搬 Vercel

**下一個模組**
- [ ] 年度行事曆（`AnnualTasks`）：欄位最少，用來驗證「在殼裡加一個模組」的流程

## 驗收清單（每次改完照跑）

沒有自動測試，這是人工替代品。修 bug 前先在這裡寫下重現步驟。

1. 打開 `app.html`，登入後不停在「載入中」
2. 標籤顯示「唯讀」或「可寫入」，跟預期一致
3. 左側九個項目都在，點每一個都有反應
4. 展覽 → 年度分頁可切換，統計數字與 SharePoint 清單筆數一致
5. 展覽 → 點任一列進詳情，三個分頁都能切
6. 詳情頁的前置階段標籤，綠灰狀態與清單資料一致
7. 儀表板顯示下一場與前置階段未完成的場次
8. 視窗縮到 820px 以下，選單收起、漢堡可開關
9. 手機實機開一次，展覽列不擠、可點
10. 瀏覽器 Console 沒有紅色錯誤
11. 手機（LINE 或 Teams 內建瀏覽器）開啟根目錄網址，轉向登入後能回來並讀到資料
12. 載入失敗時不會顯示空的統計卡，且「重新載入」按鈕有效
14. **配色改動專用**：儀表板四張統計卡的左側色線都看得到，四個數字都清楚
15. 側邊欄選中項是深藍色塊（不是黑色），品牌名前有一顆黃色小方塊
16. 展覽列四種狀態的左側線都看得見，尤其「不適用」那條灰線
17. 詳情頁前置階段的綠／灰標籤，文字都是深色（不是彩色）
18. 年度分頁選中的那顆是深藍底白字
19. **接新清單專用**：展覽總覽讀到 8 筆，年度分頁只有 2026（＋2027 灰掉）
20. 每一列的階段標籤是四顆；國內場第四顆寫「布展」，國外場寫「運輸」
21. 詳情 → 基本資訊：展覽類型、業務參展人都有值，沒有殘留的空欄位
22. 詳情 → 預算與補助：三筆費用分開列，合計旁邊有「N 欄空白」的註記
23. 詳情 → 文件與素材：三個資料夾連結點得開（開到空資料夾是正常的）
24. 儀表板在展覽分頁切過年度後，數字不變，卡片標題寫著年度
25. **兩行式版面專用**：階段標籤有沒有勾一眼看得出來（實心綠 vs 白底虛線框）
26. 國內是實心徽章、國外是空心描邊，兩者同為青石藍色系
27. 已結案的左側色槓是綠色，與上方統計卡的「已結案」同色
28. 列表上沒有右側狀態標籤（狀態靠分組標題與色槓）
29. 攤位號緊貼倒數左邊，八列對齊成一直線；沒攤位號的場次留空但倒數不位移
30. 第二行的城市各列對齊（靠等寬數字，不靠固定欄寬）
31. **把瀏覽器拉到 900px、600px、430px**：都不切字、標籤不折行
32. **文件庫讀取專用**：三個階段各顯示「資料夾是空的」（目前確實都空）
33. 丟一個測試檔進 `01_設計產出`，重開詳情頁應看到檔名、上傳時間、大小
34. 點檔名會在 SharePoint 開啟該檔
35. 把某一場的 `FolderKey` 故意改錯，應顯示「找不到這個資料夾」而不是空白
36. 上傳檔案後回到同一場，不重新載入網頁也要看得到（快取每次開啟都清）

## 2026-08-11　展覽清單重建（SharePoint 端完成，前台待改）

### 做了什麼
- 刪除舊 `Exhibitions` 清單（13 筆），已備份成 Excel
- 建立新 `Exhibitions`，ID `01765d4f-5cb9-4a5b-b04f-a9be79f5960e`，26 欄
- 寫入 8 筆 2026 年度資料
- 建立 `展覽/2026/{FolderKey}/{01_設計產出,02_現場照片,03_結案報告}` 共 34 個資料夾
- eric_ho、karen_sung 加入 Members

### 推翻的定案
- 骨幹規劃第五節第 4 條「展覽是一張扁平登記表加三個勾，刻意不追蹤展後階段」**前提不成立**。
  實際流程是六段：報名作業→設計產出→備品打包→到位→展期→展後，展後有實際工作。
  但該條的結論（展覽不與專案合併）仍然成立，靠的是另一半理由。
- 「到位」不設欄位：展覽前一天必然到現場，不可能忘記的事不需要記。

### 踩到的坑
- 舊清單的日期欄位用「日期及時間」，畫面顯示比實際值早一天。新清單一律「僅日期」，
  且寫入時用 T12:00:00Z，任何時區偏移都不會改變日期。
- `CreateFieldAsXml` 的 `Options:8` 沒有把欄位加進預設檢視，要另外呼叫 `addviewfield`。
- 舊清單的 `ExhibitionType` 實際裝的是產品線（成衣／面料），不是參展形式。
  我在填寫表範例裡瞎猜寫「貿協團」，差點讓這個欄位被誤刪。**範例值會被當成規格。**
- 舊清單的 TITAS 三個連結指向個人 Google Drive，是測試資料。
- 舊清單金額是總額且海外場為外幣；新表拆成攤位／運費／裝置三欄且統一台幣。

### 待處理
- `index.html` 八處改動（清單 ID、移除參展性質、Budget 拆三欄、
  NeedClosingReport→ClosingReportDone、連結欄位改 FolderKey 路徑、
  前置階段三顆改四顆、儀表板 currentYear 耦合、annual desc 的「九項」）
- Chrome 擴充功能必須在每個對話裡各自啟用一次，這不是設定問題

## 2026-08-11（第二次）　前台接上新清單（八處改動）

### 做了什麼

上一則列的八處，全部改完。另外多做兩件（下面說明）。

| # | 改動 | 位置 |
|---|---|---|
| 1 | 清單 ID 換成 `01765d4f-…` | `LISTS.exhibitions` |
| 2 | 移除 `ExhibitionType`（參展性質），改顯示 `Scope`（展覽類型） | `FIELD_MAP`、列名小標籤、詳情基本資訊 |
| 3 | `Budget` 一欄拆成 `BoothFee`／`FreightFee`／`SetupFee` | 預算與補助分頁 |
| 4 | `NeedClosingReport` → `ClosingReportDone`，語意從「要不要做」翻成「做完沒」 | 文件分頁新增可點勾 |
| 5 | 四個連結欄位刪掉，改用 `FolderKey` 組出資料夾網址 | `folderUrl()`／`folderCard()` |
| 6 | 前置階段三顆改四顆（加 `PackDone`） | 列表標籤、詳情、儀表板判斷 |
| 7 | 儀表板不再共用展覽模組的 `currentYear` | `dashboardYear()` |
| 8 | 年度行事曆的說明移除沒有根據的「九項」 | `MODULES.annual.desc` |

多做的兩件：

- **`ExecutionUnit`（執行單位）一起移除。** 它不在新清單的 26 欄裡，留著只會讀到 `undefined`。
  八處清單漏列這一項，但不移除的話詳情頁會出現一個永遠是「-」的欄位。
- **`IsDeleted` 過濾。** 骨幹規劃五之二第 4 條早就定了軟刪除，但舊清單沒有這個欄位，
  所以規則一直沒地方落實。新清單有了，`loadData()` 加一行過濾掉。
- **載入失敗畫面移除 `archive/sp.html` 的連結。** `sp.html` 是 Google 時期的測試檔，
  從來不是設計出來的退路。它寫死已刪除的舊清單 ID，也讀 `Budget`、`PhotoFolderUrl`
  等已不存在的欄位，換 ID 也修不回來。現在是邊用邊修，不需要另一個版本墊在下面。
  改成只留「重新載入」與「截圖給我，錯誤訊息在上面那一行」。驗收清單第 13 條一併刪除。

### 為什麼這樣決定

**資料夾網址是算出來的，不是存出來的。**
路徑固定為 `Shared Documents/展覽/{年度}/{FolderKey}/{子資料夾}`，
前台用 `Year` ＋ `FolderKey` 兩欄組出來。少四個欄位要維護，也不會有人忘記貼連結。
路徑已用 Graph 實際查過 `2026_10_TITAS`，構出來的網址跟 Graph 回傳的 `webUrl`
逐字元對得上（差別只有中文段落被 percent-encode，指向同一個資源）。

**但前台看不到資料夾裡有沒有檔案。**
「檔案存在＝該階段完成」是下一階段的事，要另外去讀文件庫。
這一版只給入口，並在分頁底下明講「資料夾一定存在，裡面有沒有檔案要點進去看」。
不要讓畫面暗示它知道它其實不知道的事。

**第四顆階段標籤的字面隨 `Scope` 變：國內顯示「布展」、國外顯示「運輸」。**
底下是同一個 `PackDone` 欄位。依填寫表「欄位規格」那張工作表對 `Scope` 的說明實作。
**這是這次唯一照規格表、而非照上一則 DEVLOG 字面（「備品打包」）做的地方**——
如果想一律顯示「備品打包」，改 `stageLabel()` 一個函式就好。

**結案簡報不放進前置階段那一排。**
它是展後的事，混進「前置階段」會讓那一排名不符實。放在文件分頁的結案報告下面，
同樣是規則 1（可逆的單一欄位，點了就存）。

**三筆費用不自動當成完整成本。**
規格明講空白不等於 0（可能由別的單位付、或還沒確定）。合計只加有數字的欄位，
並在旁邊註明有幾欄空白。寧可畫面囉唆，不要給一個看起來精確的錯數字。

**儀表板的年度自己算。**
舊版共用 `currentYear`，在展覽分頁點了別的年度，回儀表板數字也跟著變，
但儀表板沒有年度分頁，看不出來它在算哪一年。改成「今年有資料就用今年，否則用最後一個有資料的年度」，
並把年度寫在卡片標題上。

### 踩到的坑

- `toggleStage()` 原本用 `STAGE_FIELDS[en]` 反查中文鍵名，但結案簡報不在
  `STAGE_FIELDS` 裡（它不是前置階段）。改成查 `FIELD_MAP`，兩種勾都能用同一個函式。
- 文件庫路徑是 `Shared Documents`（有一個空格），不是網站介面上顯示的「文件」。
  組網址要用 `%20`，直接放空格在某些瀏覽器會斷。
- `FIELD_MAP` 右邊的中文只是程式內部的鍵名，不是畫面上的字。這次沿用舊鍵名
  （例如 `City` 仍對 `地點城市`，不改成清單顯示名「城市」），避免動到十幾處讀取。

### 用 Chrome 驗過的四件事（不是推論）

在 SharePoint 用 REST 查了新清單，四件都確認：

1. **26 個欄位的內部名稱與 `FIELD_MAP` 逐字吻合**，沒有 `Scope0` 這類重建時的後綴。
   這是最會靜默出錯的地方——名字錯了整欄空白，不會報錯。
2. **`StartDate`／`EndDate` 的 `DisplayFormat` 是 0（僅日期）**，值存成 `T12:00:00Z`。
   舊清單偏一天的坑沒有重現。
3. **三個選擇欄位的選項字面完全吻合**（`Scope` 國內／國外、`Status` 四個、
   `SubsidyStatus` 四個），且 `FillInChoice` 都是 false。`STATUS_COLOR` 對得上。
4. **算出來的資料夾網址真的打得開**：實際開了
   `.../展覽/2026/2026_10_TITAS/01_設計產出`，SharePoint 轉成正式檢視網址，
   麵包屑正確，資料夾是空的（檔案還沒放，符合預期）。

順手修掉一個自己寫出來的錯：`fmtMoney()` 與費用合計原本用 `!v` 判斷空白，
會把真正填 0 的欄位當成空白。空白與 0 是兩件事——空白是「別人付或還沒確定」，
0 是「真的不用錢」。改成明確比對 `''`／`undefined`／`null`。

### 上線後實測（用 Chrome 在正式網址上跑，不是看程式碼）

上傳後在 `https://yulunyeh0731.github.io/` 實跑一次，**沒有發現任何要修的地方**。

- 登入：SSO 直接過，沒有跳密碼、沒有跳同意畫面。重新載入後靜默登入，不必再點
- 模式標籤顯示「唯讀」——與預期一致，寫入權限確認仍未核准
- 儀表板：卡片標題「展覽 · 2026 年度」（改動 7 生效，年度看得出來）；4／0／4 共 8 筆
- 儀表板「前置階段未完成 3 場」列出 TITAS、Performance Days、Functional Fabric Fair。
  第四場 09 月上海四顆全勾所以沒被列 → **`PackDone` 確實被算進去了**
- 展覽總覽：年度分頁 2026 有資料、2027 灰掉；每列四顆階段標籤，
  **國內的 TITAS 第四顆寫「布展」，國外場寫「運輸」**；
  Functional Fabric Fair 的「繳費」是灰的，對應 `PayDone=FALSE`
- 詳情 → 基本資訊：展覽類型、業務參展人都有值，沒有殘留的空欄位
- 詳情 → 預算與補助：TITAS 三欄都有值 → 合計 848,000，無空白註記；
  10 月 Performance Days 只有攤位費 → 「合計 NT$ 322,014（2 欄空白）」
- 詳情 → 文件與素材：三個資料夾入口 ＋「結案簡報未完成」的灰勾。
  從網頁點 `01_設計產出` 開新分頁，落在
  `展覽/2026/2026_10_PerformanceDays/01_設計產出`——展覽對、子資料夾對
- 窄螢幕 430px：漢堡出來、選單收起、版面沒擠壞
- Console 無錯誤（重新載入後重讀確認）

**唯一無法實測的一條：驗收清單第 24 條（儀表板年度脫鉤）。**
目前只有 2026 一個年度有資料，年度分頁切不動，所以舊碼與新碼會給出相同結果。
這條要等有第二個年度的資料才驗得到——不要因為「畫面看起來對」就把它當通過。

### 待處理

- [ ] 檔案還沒放進資料夾，所以連結目前一定是空的。放了之後不用改程式
- [ ] 寫入權限仍是唯讀（已在正式網址上確認）。核准後階段標籤與結案簡報才點得動，
      程式不用改——`probeWrite()` 會自動偵測
- [ ] 骨幹規劃五之四表格「展覽」那一列要補：勾報名／繳費／選位／備品打包／結案簡報
- [ ] 驗收清單第 24 條要等 2027 年度資料才驗得到

## 2026-08-11（第三次）　展覽列表版面重排

### 起因

上線後她逐項回報視覺問題，我逐項局部修補了五輪都沒解決，最後她說
「你根本想錯了，原本後面還有東西，請全盤考慮」。
**這句糾正是對的，而且是這一則的重點。** 我每次只看她圈起來的那一塊，
從來沒有回頭問「這一列到底該放什麼」，所以它每換一個寬度就壞一次。
根本問題不是排版技巧，是這一列裝了七樣東西。

### 全盤盤點的結果：狀態被講了三次

| 元素 | 傳達什麼 | 判斷 |
|---|---|---|
| 分組標題「確定參加（4）」 | 狀態 | 留，狀態的主要載體 |
| 左側色槓 | 狀態 | 留，安靜不佔位 |
| 右邊的狀態標籤 | 狀態 | **刪，同一件事第三次** |
| 展覽名稱／國內外／展期／城市／倒數／階段標籤 | 各自唯一 | 留 |
| 攤位號 | 查詢用不是掃描用 | 留，但移位 |

同一組四列的狀態標籤字一模一樣，掃過去零資訊量。拿掉它之後空間就夠了——
這回頭證明了先前擠爆的原因是塞太多，不是排版技巧不夠。
按她自己的判準推是同一個結論：儀表板原則是「只顯示需要採取行動的項目」，
同一套紀律套到列上就是「只顯示會被掃的東西」。

### 六處改動

| # | 改動 | 為什麼 |
|---|---|---|
| 1 | `--green-soft` `#F1F6F3` → `#DCEBE3` | 見下方「數值上的錯」 |
| 2 | `.stage-on`／`.stage-off` 改實心對空心 | 差異不只押在色相上 |
| 3 | `.nature-badge` 拆 `.nb-dom` 實心／`.nb-int` 空心 | 國內外要分得出來 |
| 4 | `.ex-row` 單行 flex → 兩行式 | 消滅欄位對齊問題，不是修好它 |
| 5 | `exRowHtml` 移除狀態標籤，攤位號移到倒數左邊 | 見上方盤點 |
| 6 | `STATUS_COLOR` 已結案 `gray` → `green` | 與統計卡一致，且與暫不參加分開 |

順帶刪掉 `statusTag()`——列表不再顯示狀態標籤後它就沒人用了，成了死碼。
`.tag` 那組樣式保留，詳情頁右上角還在用。

### 數值上的錯：我上次配色改動留下的洞

舊的 `--green-soft: #F1F6F3` 與 `--gray-soft: #F3F3F1` 亮度比只有 **1.02 : 1**。
我當時給階段標籤設了「不同色底 ＋ 相同文字色」，結果那個「不同」在數值上等於零，
所以有沒有勾的差別根本不存在。**已結案與暫不參加也是同一組值，兩個意思相反的狀態長得一樣。**

但要修正我自己的診斷說法：**亮度比不是這裡的正確判準。**
`--amber-soft` 與 `--gray-soft` 的亮度比也只有 1.04，可是琥珀一眼就看得出來。
真正的問題是**彩度**——`#F1F6F3` 三個通道差距只有 5（R241 G246 B243），
實質上是中性灰，所以無法與另一個中性灰區分。`#DCEBE3` 差距 15，是看得出來的綠。

改一個變數，階段標籤與狀態標籤兩層同時修好。`--green-bg`（統計卡底色）維持原值，
那裡有左側色線，沒有並排比較的問題。

### 為什麼兩行式比「加固定欄寬」對

第一次的提案是七欄固定寬的 grid。做成樣張給她看，**在窄畫布下壞得比原版更難看**：
名稱被切、國內外徽章整個不見、「報名」被折成兩行。固定像素欄寬撐不住寬度變化。

兩行式沒有任何固定欄寬。第二行左邊是一串文字，日期格式固定 13 字元
配上 `font-variant-numeric: tabular-nums`，所有列的日期同寬，後面的城市自然對齊。
**對齊是從「不需要對齊」得到的，不是靠對齊技巧得到的。**

唯一的固定寬度是 `.ex-count` 的 `min-width: 76px`，讓攤位號一律緊貼倒數左邊而各列對齊。
76px 是為三位數天數留的餘裕（目前最大 77 天）；就算出現三位數，那一欄只會整體往左推，仍是對齊的。

### 兩個刻意沒做的

- **攤位號不加「攤位」二字。** 她說看號碼就知道，`M2501`、`H42+H46` 這種格式
  不會被誤認成別的東西。
- **已結案那幾列右邊的「已結束」留著。** 它是第三次講「這是過去的事」，
  但有一個用處：如果某場還在「確定參加」卻已過展期，那顆「已結束」就是在提醒該結案了。
  壓成最淡的灰，沒有拿掉。

### 待處理

- [ ] 上傳後用 Chrome 實跑，重點看窄螢幕：這一版沒有固定欄寬，理論上不會擠爆，
      但那是推論，要實際拉窄看過
- [ ] 統計卡的綠（`--green-bg`）要不要跟著加深，待她決定

## 2026-08-11（第四次）　文件庫讀取：頁面直接列出每個階段的檔案

### 起因與定案的維護方式

討論「展覽內容怎麼維護」時，原本的模式要人記三件互相依賴的事
（先建資料夾、再建列、`FolderKey` 兩邊逐字一致），她指出太繁瑣。
盤過一輪後定案：

- **上傳與下載都走 SharePoint，不做頁面上傳。** 她的使用情境以電腦為主，
  頁面上傳唯一的決定性優勢（手機上找資料夾很痛）不存在；
  剩下的全是缺點——大檔要自己寫分段上傳、整批照片最容易壞、
  檔名衝突與版本歷程都要自己處理，而且權限沒到之前一行都測不了。
  實際做法：從頁面的資料夾連結點進正確的子資料夾，
  或按「新增 OneDrive 捷徑」讓它出現在檔案總管，之後拖進去就好。
- **頁面負責「看得見」**：列出每個階段實際有哪些檔案、什麼時候上傳、多大。
  這只需要 `Sites.Read.All`，不用等 IT。

### 三個被否決掉的設計（記下來免得重複討論）

1. **設計產出的完成勾（`DesignDone`）與品項多選（`DesignItems`）——不做。**
   問到設計需求的來源是「業務同事發信或訊息來我們就做」，這推翻了整個前提：
   需求是陸續進來的、不可預測，沒辦法在開場時勾一份清單，
   而「設計產出」這一段在開展前永遠不會結束——今天定稿隔天又來一張社群圖。
   它不是關卡，是一條流到開展的水流，沒有完成訊號可存。
   **而且照她自己的判準，這是受理登記的形狀（有人丟給你、有明確完成點），
   不是展覽的欄位。** 骨幹規劃第 4 條早就警告過不要把東西硬塞進展覽模組。
2. **逐項確認的標籤——不做。** 做成樣張給她看，八個品項有六個是灰的，
   但灰的不代表沒做（`攤位_3D_草稿.skp` 明明在資料夾裡）。
   這種標籤撐不過三週就會全部停在灰色變裝飾，正是她那條
   「需要打字的狀態欄位三週後會是空的」。
3. **自動化檔名——不做。** 能自動化的部分正好是不需要的：
   「哪一場的哪個階段」路徑已經講完了，加前綴只是把路徑抄一次到檔名上；
   「這是什麼東西」程式無論如何不知道。
   替代方案是文件庫加「類型」欄位，但那又是一個要靠人維護的欄位。
   結論：靠可見性——檔名列出來之後爛檔名自己會現形，改名是一次點擊。
   可見性比規則有效，因為不需要任何人記得遵守，也不會過期。

**我在這一輪犯的錯：用「內部名稱建了改不掉，要現在決定」給她製造了時間壓力。
那個壓力是我自己憑錯的前提造出來的。** 實際上什麼欄位都不用建，沒有任何一件事有時間壓力。

### 實測過才寫的程式

寫之前先在正式網址上用網頁自己的權杖打 Graph，確認三種情況：

| 情況 | 回應 |
|---|---|
| 資料夾存在有內容 | 200，帶陣列 |
| 資料夾存在但空的 | 200，零筆 |
| 路徑不存在 | 404 |

三種對使用者的意思完全不同（「還沒放」與「找不到」不能混為一談），所以分開處理，
外加讀取失敗共四種狀態各有明確訊息。

同時把靜默失敗的風險一次核對掉：**八場的 `FolderKey` 與文件庫實際資料夾名稱逐字吻合，
三個子資料夾都在，沒有孤兒資料夾。**

### 新增的東西

- `driveList(segments)`：讀資料夾內容，四種狀態，回傳 `{ state, items }`
- `folderCache`：**每次開啟詳情都清掉重讀。** 你剛在 SharePoint 丟完檔案回來，
  最重要的就是看到它在裡面；三個請求的成本遠低於顯示過期內容
- `renderFolderFiles()`／`renderAllFolders()`：每筆檔案可直接點開，顯示上傳時間與大小
- `.doc-row` 系列樣式，窄螢幕時時間與大小換到第二行

### 踩到的坑

**我在加文件庫函式時，`str_replace` 的 `old_str` 選了 `let toastTimer = null;`
當定位點，結果把那行宣告換掉了。** `node --check` 抓不到——語法完全合法，
但第一次呼叫 `toast()` 就會在 `clearTimeout(toastTimer)` 丟 ReferenceError。
是靠「關鍵宣告還在不在」的自我檢查抓到的。
**教訓：不要拿要保留的程式行當插入定位點。**

### 上線後實測（正式網址，Chrome）

驗收清單 25～36 全部通過，**沒有發現需要修正的地方**。

- 25～30：階段標籤實心／空心一眼分得出來；國內實心、國外空心；已結案左槓綠色
  與統計卡一致；列表已無狀態標籤；八列的日期、城市、攤位號、倒數全部對齊；
  09 月上海沒有攤位號，留空但倒數不位移
- 31：**900px 與 430px 都不切字、標籤不折行。** 900px 特別值得記——
  那正是前一版七欄 grid 壞掉的寬度
- 32：三個階段都顯示「資料夾是空的，還沒有檔案。」
- 33、34：她丟了一個 `S__19439651.jpg` 進 `01_設計產出`。
  頁面顯示「共 1 個檔案」＋檔名＋`2026/08/11 19:21`＋`214 KB`，點檔名在
  SharePoint 正確開啟該檔
- 35：**沒有真的去改清單**，而是直接呼叫 `renderFolderFiles()` 餵一個不存在的
  資料夾代號，走同一段程式。代號打錯與年度打錯都回 `missing`，
  畫面顯示「找不到這個資料夾。可能是資料夾代號與文件庫上的名稱不一致。」
  這條最重要——那條路徑原本會靜默失敗
- 36：**沒有重新載入網頁**（頁面時間戳仍是前一次載入的 6:19:41），
  直接開詳情就看到剛上傳的檔案。快取每次開啟清除有效

唯一的 console 錯誤來自 AdGuard 擴充功能，不是這支程式。

### 待處理
- [ ] **網站權限還沒設，這件事現在正在擋住「業務同仁自己抓檔」。**
      「所有人（除了外部使用者）」加進 Visitors 之前，除了 eric_ho、karen_sung
      沒人點得開那些連結
- [ ] 檔案連結用的是 `webUrl`（在 SharePoint 開啟）。
      如果想要點了直接下載，要改用 `@microsoft.graph.downloadUrl`，
      但那個網址有時效，不能存起來重複用
- [ ] 骨幹規劃第八節要補：受理登記那張清單需要一個指向展覽的欄位
      （例如 `RelatedExhibition`），否則「這場展覽業務要求了哪三件事」查不出來。
      現在不做，但欄位一建就改不掉，所以要先寫下來

## 2026-08-12　拆成共用檔 ＋ 兩個入口

### 定案的架構變動

| | 網址 | 誰用 | 看到什麼 |
|---|---|---|---|
| 工作台 | `/` | 你 ＋ 部門同事 | 全部模組 |
| 展覽檢視 | `/exhibitions/` | 全公司 | 只有展覽，無側邊選單 |

**這推翻骨幹規劃第 1 條（一個殼＋N 個模組，不是 N 個獨立 HTML 檔）
與第 10 條（不各做一套介面）。**

那兩條的理由是避免她自己的工具散成 N 個網頁。這次切的不是模組，是**觀眾**——
一個給部門、一個給全公司，數量固定為二，不隨模組增加。不會重新引進當初要避免的分散。

換來的東西那兩條規則做不到：**資料的保護完全不依賴介面。**
原本的方案是同一頁靠角色判斷隱藏選單，但那是假界線——`index.html` 是公開的文字檔，
判斷在使用者自己的瀏覽器裡執行，F12 刪掉 class 就出現了，而且他們根本可以直接開
SharePoint。現在不需要角色偵測、不需要名單、不需要 AppRoles 清單。

### 檔案結構

| 檔案 | 內容 |
|---|---|
| `app.css` | 全部樣式（含殼樣式，公開頁用不到但無害） |
| `core.js` | 登入、Graph 讀寫、文件庫讀取、`boot()`／`loadData()` |
| `exhibitions.js` | 展覽模組的**版面** ＋ 全部繪製程式 |
| `index.html` | 殼：選單、`go()`、儀表板 |
| `exhibitions/index.html` | 公開頁：無選單，直接顯示展覽 |

**版面也放進 `exhibitions.js`（`EX_MARKUP` 模板字串），不是只放程式。**
這是關鍵——如果版面複製成兩份 HTML，改一邊忘記另一邊只是時間問題。
她光是列的版面就改過六輪，這個風險是實際的。

兩個約定寫在 `core.js` 開頭：
1. **各頁面必須在載入 `core.js` 之前自行宣告 `REDIRECT_URI`。**
   Entra 對重新導向 URI 是完全比對，兩個網址必須各自註冊、各自宣告。
   寫在共用檔裡會讓其中一頁永遠登不進去。
2. **各頁面必須自行定義 `onDataLoaded()`。** 工作台去儀表板，展覽檢視直接顯示展覽。

### 已驗證的事實（不是推論）

用 Chrome 讀 Entra 與 SharePoint 確認：

- **她是 `展覽管理儀表板` 這個應用程式的唯一擁有者**，「新增重新導向 URI」按鈕可用
  → **改 Entra 不必找 IT。**
- SharePoint 現況：Owners = 她；Members = eric_ho、karen_sung；**Visitors = 空**；
  `Exhibitions` 清單、`展覽` 資料夾、文件庫的**獨立權限全部是 false**
  → 權限全部從網站繼承，**目前只有一個開關**：加進 Visitors 就是整個網站唯讀。
  「只看展覽」這個選項現在不存在，必須中斷繼承才能造出來。

### 拆分時抓到的一個 bug

`toggleStage()` 裡有 `renderDashboard()`，但儀表板屬於殼。共用檔在公開頁執行到那一行
會丟 ReferenceError。改成 `if (typeof renderDashboard === 'function')`。
**這類錯誤是「跨檔案引用」特有的，單檔時代不會出現**——`node --check` 也抓不到，
是靠「把所有被呼叫的名字對照所有已定義的名字」這個檢查抓到的。

### 應用程式名稱要決定

同意畫面上顯示的是 **「展覽管理儀表板」**，業務同事第一次登入就會看到。
對公開頁剛好合適，但對工作台已過時。建議改成中性的（例如「TEX-RAY 工作台」）。
不急，但要在業務同事登入之前決定。

### 上線後實測（兩頁都跑過）

- Entra 已新增 `https://yulunyeh0731.github.io/exhibitions/`（單頁應用程式），
  她自己按的，兩筆重新導向 URI 都在
- 五個檔案上傳後從 `raw` 抓下來逐位元組比對，全部相同
- **工作台**：登入、儀表板、展覽模組（由 `exhibitions.js` 掛載）全部正常
- **公開頁**：無側邊選單、無唯讀標籤、標題「展覽資訊」，
  清單／統計／詳情／文件庫讀取全部正常。同一份版面在兩頁呈現一致
- 兩頁 console 都無錯誤

### 拆檔造成的一個 bug（已修）

**公開頁只出現年度分頁，展覽清單是隱藏的。**
`renderYearView()` 只填內容，把 `#view-main` 的 `hidden` 拿掉的責任原本在殼的
`go('exhibitions')` 裡。公開頁沒有 `go()`，**這個責任在拆檔時掉在兩者之間。**

修法是在 `renderYearTabs()` 結尾補 `showMain()`，兩頁行為就一致了。
**刻意不放在 `renderYearView()`**——`toggleStage()` 在詳情頁裡也會呼叫它，
那會把人踢回列表。`renderYearTabs()` 只在載入時跑一次，放那裡才安全。

這類「誰負責顯示」的錯誤是拆檔特有的：兩邊的程式都沒壞，壞的是責任歸屬。

### 三個踩過的坑（都不是程式問題）

1. **GitHub Pages 建置卡住。** #36 的 `build` 工作一直是 `queued`，
   一個步驟都沒跑，整個流程 10 秒被判失敗，所以 Pages 停在舊版、
   新檔案全部 404。**判斷方法：看 `build` 有沒有步驟記錄——沒有記錄就是沒跑到，
   不要去找檔案的問題。** 重跑同一個 run 沒用（同一個排隊項目），
   用新的 commit 建立新的排隊才通。
2. **她的瀏覽器擴充功能會往頁面注入兩個 `<script>`。**
   這害我一度以為 Pages 提供的檔案內容不對。用瀏覽器驗證檔案內容時要扣掉這一層。
3. **`.js` 檔在她的環境不好下載**，改提供 `.txt` 副本，或直接在 GitHub 網頁上編輯。

**我在這一段連續三次推測都錯**（先猜瀏覽器快取、再猜 Jekyll 樣板語法、
再猜檔案被截斷）。真正找到答案的都是去查事實：Actions API 的 job 狀態、
字元數與 UTF-16 長度換算、標記計數。**沒看到證據前不要叫她動手改設定或加檔案。**

### 待處理（按順序，不可跳）
- [ ] **她按**：`Exhibitions` 清單與 `展覽` 資料夾中斷權限繼承，
      各自加**一位**業務同事為「讀取」。網站 Visitors 保持空的
- [ ] 請那位業務同事實測公開頁。**清單層級權限能不能透過 Graph 讀到，我沒實測過**，
      不通就只能退回全站唯讀
- [ ] 通了再擴大到「外部使用者以外的所有人」

## 2026-08-12（第二次）　修 state_not_found：按登入沒反應

### 症狀

她自己測的時候，兩頁都是「按了登入完全沒反應」——連「正在前往 Microsoft 登入頁…」
那行提示都沒出現。

### 診斷過程（重要：一開始查不到，因為我自己是已登入狀態）

我的瀏覽器工作階段一直有登入紀錄，所以永遠走靜默登入那條路，
**測不到「從未登入狀態按登入」這條路徑**。把 `sessionStorage` 清掉之後一次就重現。

重現後在頁面上直接查，拿到決定性的證據：
`sessionStorage` 裡只剩一個 `server-telemetry-{clientId}`，內容是

```
{"failedRequests":[865,"...",865,"..."],"errors":["state_not_found","state_not_found"],"cacheHits":0}
```

她的 console 也印出同一件事：`handleRedirectPromise: state_not_found: State not found: Cached State`。

### 原因

MSAL 在跳去 Microsoft 之前，會把核對值（state）存進 `sessionStorage`，
跳回來要用它比對回應是不是自己發出去的。**某些情況下那筆值在跳轉過程中會被丟掉**，
回來就是 `state_not_found`。

而舊程式在這裡**只把錯誤 `console.warn` 出來、把訊息塞進提示，沒有清掉殘留**。
殘留會讓 MSAL 在後續每次 `loginRedirect()` 都丟 `interaction_in_progress` 並自己攔下來——
**畫面上完全沒反應，使用者只會以為按鈕壞了。** 這就是症狀的來源。

**這不是拆檔造成的。** `REDIRECT_URI`、`msalApp` 設定、`signIn` 函式我全部驗過都正確，
直接呼叫 `loginRedirect()` 也確實跳去 Microsoft 又跳回來。
DEVLOG 8/4、8/5 連三則都在處理登入跳轉——這是舊傷。

### 三處修正（只動 `core.js`）

1. **`storeAuthStateInCookie: true`**。核對值額外存一份到 cookie，撐過跳轉。
   這是這個錯誤的正規解法。**權杖本身仍然只在 `sessionStorage`**，
   關掉分頁就消失，不會留在那台電腦上。
2. **新增 `clearMsalState()`**：只清 MSAL 自己的鍵（`msal*`、`server-telemetry*`、
   含 clientId 的）與核對值 cookie，不碰別的東西。
3. **自動復原**：
   - `boot()` 遇到 `handleRedirectPromise` 失敗 → 清殘留、重載一次。
     **重試記號用 `localStorage`（`wb-login-retry`），因為 `clearMsalState()`
     會清 `sessionStorage`，記號放那裡會被自己清掉，變成無限重載。**
   - `signIn()` 遇到 `interaction_in_progress` 或 `state_not_found`
     → 清殘留、自動再試一次。
   - 兩者都失敗才顯示訊息，而且要告訴使用者能怎麼做（把分頁全部關掉再開），
     不是丟一串英文錯誤。

### 為什麼一定要自動復原，不能靠說明

**業務同事遇到這個狀況時，不可能請每個人開 F12 貼指令。**
而且 Chrome 現在預設阻擋在 Console 貼上程式碼（要先手動打 `allow pasting`），
連她自己都貼不進去。任何需要開發者工具才能解的問題，等於沒解。

### 待驗證

- [ ] 上傳 `core.js` 後，**必須在清空 `sessionStorage` 的狀態下測登入**，
      否則會像我一開始那樣走靜默登入、測不到這條路徑
- [ ] 兩頁都要測（共用同一份 `core.js`）
