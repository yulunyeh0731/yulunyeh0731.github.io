# 工作台｜骨幹規劃 v1

> **用途**：這份文件定義整套系統的骨幹——有哪些模組、每個模組對應哪張清單、
> 什麼順序建、以及所有已定案的決策與判準。
>
> 搭配 `展覽管理系統_交接說明_v2.md` 使用：那份是第一個模組的實作細節，
> 這份是整套系統的架構。新對話請兩份一起讀。
>
> 使用者：企劃課長（`yulun_yeh@texray.com` / `T0621@texray.com`），
> 同時兼總務課長。不具程式背景。
>
> **最後更新：2026-08-11。** 本次改動集中在第三節、第五節第 4 條、第五之四、
> 第五之五、第六節之後新增的第六之二、第七節、第八節、第十節。
> 所有改動都標了「（2026-08-11 修正）」或「（2026-08-11 新增）」。

---

## 零、名稱

**系統名稱是「工作台」**，副標「Yulun ・ 企劃與總務」。

刻意不叫「TEX-RAY 工作系統」——那個名字會招來「公司級系統」的期待，
而使用者只有 7～8 人。「工作台」是個人尺度的比喻，不宣稱自己是系統，
而且不綁部門、職務變動也不用改。

主標不會變，副標會變，所以分成兩行。名稱出現在三處：
瀏覽器標題、側邊欄品牌、登入卡。

## 一、一句話架構

一個 SharePoint 網站放所有資料，一個 HTML 殼放所有介面。
**一個模組 = 一張 SharePoint 清單 ＋ 殼裡的一段繪製程式。**

所以「建骨幹」＝ 決定要建哪幾張清單、叫什麼、什麼順序。

## 一之二、實際位置（2026-08-11 新增）

這一節放 GitHub 上讀不到、但每個新對話都需要的事實。

| 項目 | 值 |
|---|---|
| SharePoint 網站 | `https://texray.sharepoint.com/sites/internal-tools`（顯示名稱「內部工具」） |
| Graph 網站 ID | `texray.sharepoint.com,523489e7-e84e-4554-96f9-2a27018db21a,fc5dd947-527e-47f8-8762-02c81a22d02a` |
| `Exhibitions` 清單 ID | `01765d4f-5cb9-4a5b-b04f-a9be79f5960e` |
| 文件庫路徑 | `/sites/internal-tools/Shared Documents`（顯示名稱「文件」） |
| 展覽資料夾根 | `文件/展覽/{年}/{FolderKey}/` |
| 前台 | `https://yulunyeh0731.github.io/`（repo `yulunyeh0731/yulunyeh0731.github.io`） |

**網站的網址路徑建立時就定死、改不掉**，清單也一樣。`index.html` 只存了 Graph 的
GUID，從 GUID 推不回網址，所以網址要寫在這裡。

---

## 二、目標與範圍

**做**：個人與小團隊的工作追蹤。我和同事手上正在跑的事，現在到哪了。

**不做**：金流、採購、人事、資產折舊。公司已有 ERP（漢門）與 EIP，
這套系統對它們只存指標（編號或連結），不重複記內容。

**使用者規模**：本人 ＋ 企劃 2～3 人 ＋ 總務 3～4 人，共 7～8 人。
另有其他部門同事（業務、人資、財務）只查資料與下載。

---

## 三、骨幹：模組與清單對照

| 選單項目 | SharePoint 清單 | 狀態 |
|---|---|---|
| 儀表板 | 無（讀其他清單彙總） | 最後做 |
| 展覽 | `Exhibitions` | **清單與資料夾已建，前台待改**（2026-08-11 修正） |
| 年度行事曆 | `AnnualTasks` | 待建 |
| 受理登記 | `Requests`（一張，含部門欄位） | 待建 |
| 待辦事項 | `Todos` | 待建 |
| 專案 | `Projects` ＋ `ProjectItems` | 待建 |
| 對外資料 | `ExternalData` | 待建 |
| 分類設定 | `Categories` | 隨第一個新模組一起建 |
| 通知設定 | 無（設定寫在 Power Automate 流程裡） | 有提醒需求時再說 |

**（2026-08-11 修正）注意名稱不一致，尚未決定要對齊哪一邊**：
線上的 `index.html` 選單實際是「基礎資料」與「回收桶」兩項，
而本表寫的是「分類設定」、且沒有回收桶。見第八節。

**「人員」不建清單。** SharePoint 有「人員」欄位型別，直接從公司目錄選人。
但**單筆、只讀不查的人名用單行文字就夠**（例如展覽的業務參展人），
人員欄位要比對目錄反而慢。（2026-08-11 新增）

**選單項目可以先掛上、點進去是空的。** 骨幹先定，功能一個一個補。

---

## 四、三種形狀與歸類判準

每接到一個新工作，用這個判斷它進哪個模組。

**形狀一 — 受理登記**：有人丟給你、有明確完成點。
維修庶務、收發文、供應商建檔、資產異動、簽證申辦、桌牌看板、印刷品、禮贈品、DM 需求。

**形狀二 — 對外資料**：會過期，**而且**要重複放在不只一個對外的地方。
認證獎項、ESG 慈善、環境碳數據、海外產區文件、公協會參與。
（只有時效沒有多出口 → 年度行事曆就夠。只有多出口沒有時效 → 公檔就夠。）

**形狀三 — 專案**：有起訖、有階段、有多個並行工項。
樓層調動整修、EIP 文管中心、股東會協辦。

**外加：年度行事曆** — 不是引擎，是一張清單。
內容待確認（見第八節）。已知會落在這裡的類型：
年度或半年一次、由外部死線驅動、需要先蒐集一批內容才交得出去。

**都不是 → 不進系統。**
沒有多個工項、沒有階段推進的事不要開成專案（例如「幫董秘做兩塊桌牌」）。
這條要當拒絕條件用，不然半年後清單會塞滿五分鐘做完的事，然後你就不看它了。

---

## 五、已定案的決策

沿用 `展覽管理系統_交接說明_v2.md` 的八條，另加以下各條。
**這些都是討論後刻意的選擇，不是遺漏，請勿重新討論。**

### 架構

1. **一個 HTML 殼 ＋ N 個模組，不是 N 個獨立 HTML 檔。**
   否則會得到 N 個各自獨立的網頁，就是原本想避免的分散。
   `sp.html` 已有 `view-main` / `view-detail` 切換與 `detail-tabs` 分頁，
   加左側選單是延伸既有模式，不是重寫。

2. **改殼要在第二個工具之前，不要等到第三個。**
   兩個檔案合成殼還容易，三個以上開始痛。

3. **不走 Power Apps。** Canvas app 沒有可手寫的原始碼檔，Claude 無法直接交付；
   HTML 可以直接給檔案。對不寫程式的使用者這是決定性的。

### 模組邊界

4. **展覽與專案是兩個獨立模組，不合併。**
   （這推翻了較早「展覽當專案的第一個範本」的建議。）
   理由：`Exhibitions` 的實際欄位（BoothNo、BoothSize、補助三欄、報名／繳費／選位）
   不會出現在樓層整修上；而樓層整修需要的階段＋工項，`Exhibitions` 完全沒有。

   **（2026-08-11 修正）** 原文接著寫「加上展覽刻意不追蹤截止日與展後階段，
   它是一張扁平登記表加三個勾，不是有階段推進的專案」——**這個前提已推翻。**
   實際流程是六段，而且展後有實際工作（結案簡報、照片整理）。

   結論（不合併）仍然成立，靠的是另一半理由：**展覽的階段是固定的六段、
   不是每案自訂，工項也不需要獨立追蹤廠商與驗收。** 那才是它與專案的真正差別。

   推翻的原因值得記：原本那句是從舊清單的欄位形狀推出來的，不是從實際流程問出來的。

5. **受理登記一張清單，用「部門」欄位分。** 因為總務與企劃資料不需要隔開。
   總務與企劃的差異用**顯示邏輯**處理（總務同仁預設停在總務分頁），不用權限。

6. **對外資料的出口當欄位，不當第二張清單。**
   官網／公司 profile／工廠 profile／社群各一個布林。
   出口不會天天變，欄位夠用。注意欄位內部名稱建了改不掉，所以出口清單要先想清楚。

7. **專案是兩張清單（母子）**：`Projects` 掛主體與階段，`ProjectItems` 掛工項
   （廠商、時程、驗收各自獨立）。樓層整修的 OA／弱電／木工／清運就是工項。

### 權限與存取

8. **網站權限：「所有人（除了外部使用者）」加進 Visitors，全公司唯讀。**
   不做項目層級權限、不做角色功能矩陣。7～8 個人不需要。

   **（2026-08-11 新增）要能寫入的人必須單獨加進 Members。**
   Visitors 是唯讀，跟 Entra 的 `Sites.ReadWrite.All` 是兩層不同的東西：
   Entra 決定「網頁能不能代表登入的人寫」，網站群組決定「這個人本身能不能寫」。
   兩層都要通。目前 Members：`yulun_yeh`、`eric_ho`（企劃專員）、`karen_sung`（企劃助理）。

9. **不設 Entra「需要使用者指派」。** 它跟「全公司都能用網頁」互相衝突，
   兩者不能同時要。改以 GitHub 帳號開兩階段驗證作為主要控制，
   託管搬離個人 GitHub 列為中期目標。

10. **其他部門的人不進後台。** 他們只需要唯讀查詢與下載。
    後台只有一套，靠顯示邏輯決定看到哪些分頁，不各做一套介面。

### 讀寫分工

11. **HTML 負責「看」，SharePoint 介面與 Microsoft Forms 負責「填」。**
    - 同仁填資料 → 直接用 SharePoint 清單介面
    - 其他部門提需求 → Microsoft Forms ＋ Power Automate 寫進清單

    **（2026-08-11 修正）但「點一下就能完成的」放前台。** 判準見第六之二節。
    原文寫的「Entra 權限是 `Sites.Read.All`（唯讀），網頁不能寫入」是當時的狀態，
    寫入權限已向 MIS 申請、待核准。`probeWrite()` 會靜默偵測，核准後不需改程式。

12. **提醒走 Power Automate，發到 Teams。** 它接的是清單，跟介面無關。
    （不走 LINE——公司用 Teams。）

### 檔案與分類

13. **資料進清單，檔案進文件庫。** 清單沒有資料夾，就是列和欄。

14. **分類用欄位，不用資料夾。** 一個檔案可以屬於多個分類；
    而且分類藏在資料夾路徑裡系統讀不到。
    **例外**：天然互斥的情況可以用資料夾——一個專案一個資料夾、一場展覽一個資料夾。

    **（2026-08-11 新增）展覽資料夾的命名是「年_月_名稱」**，例如
    `2026_10_TITAS`、`2026_03_PerformanceDays`。加月份不是為了好看——
    **同一個展覽一年會有春秋兩個檔期**（Performance Days、Functional Fabric Fair、
    Functional Textiles Shanghai 都是），只用「年_名稱」會撞名。
    這個值存在清單的 `FolderKey` 欄位，前台用它組出路徑。

15. **進系統的東西，檔案一定在 SharePoint 團隊網站的文件庫**——
    不是個人 OneDrive、不是 Teams 聊天檔案、**也不是 Google Drive**（2026-08-11 補）。
    舊公檔（`J:` 網路磁碟）不搬，沒進系統的檔案留在原處完全沒問題。
    觸發器天然存在：新版一來就進 SharePoint。

---

## 五之二、寫入慣例（全系統適用）

**判斷方法全系統一致，套用後每個欄位的結果不同。** 不是統一行為。

1. **可逆的單一欄位 → 點了就存，沒有按鈕。** 狀態、勾選、負責人、日期。
2. **一組有推導關係的欄位 → 一顆按鈕，一律叫「儲存」。**
   「剛好排在同一張卡」不算一組。
3. **不可逆、或會通知別人、或有連鎖效果的 → 一定要按，用動詞命名。**
   「結案」「指派」「登錄新版本」。
4. **刪除一律軟刪除**，改 `IsDeleted` 欄位，各模組過濾掉它。

已知例外與判例：
- 對外資料的「登錄新版本」（換連結＋換有效期）歸規則 3。兩個欄位有推導關係，
  而且會讓那一行所有出口翻成「待更新」——有連鎖效果的動作不能點了就存。
- 展覽的報名／繳費／選位／備品打包／結案簡報完成歸規則 1，點了就存。（2026-08-11 修正）

## 五之三、儀表板原則

**儀表板只顯示需要你採取行動的項目。**
已完成、已結案、已放棄的一律排除，那些在各模組裡看。

這條一次定，之後每個模組不用重新討論
（受理登記的已結案、待辦的已完成、行事曆的去年項目，全部適用）。

**（2026-08-11 新增）儀表板永遠顯示「今年」，不受任何模組的分頁狀態影響。**
線上版目前有一個 bug：`renderDashboard()` 用的 `currentYear` 是展覽模組的年度分頁在改，
所以在展覽切到 2025 之後點儀表板，儀表板會靜靜地變成 2025。待修。

## 五之四、每個模組能做哪些動作

決定欄位怎麼設計，所以要在建清單之前定。

| 模組 | 可以做的事 |
|---|---|
| 受理登記 | 新增一筆、推進狀態、指派負責人、結案 |
| 待辦事項 | 指派、打勾完成、改期 |
| 專案 | 推進階段、新增工項、勾工項驗收 |
| 對外資料 | 勾「這個出口更新了」、換連結、改有效期 |
| 年度行事曆 | 勾完成 |
| 展覽 | 勾報名／繳費／選位／備品打包／結案簡報完成（2026-08-11 修正） |

**展覽前台待評估但未定案**：參展狀態與補助核銷狀態改成前台下拉直接改。
兩者都是選項欄位、點一下就能完成，符合規則 1，但還沒做。（2026-08-11 新增）

寫入權限 `Sites.ReadWrite.All` 覆蓋以上全部。
建清單與欄位不需要它（在瀏覽器上用自己的身分建）；提醒通知在網頁外面（Power Automate）。

## 五之五、刻意不採用的工具（避免重複討論）

- **不換資料庫。** SharePoint 清單換成 Postgres／Supabase 會失去四樣現在免費的東西：
  Entra 登入、網站成員資格當權限、SharePoint 版本歷程當稽核、
  以及資料落在公司自有基礎設施。以現有量，5,000 筆檢視門檻與無 JOIN
  的限制數年內碰不到。

- **不用線上 Excel 當資料源。（2026-08-11 新增，已完整評估）**
  技術上可行，而且有一個真實的好處：Claude 可以直接讀寫 Excel 檔，但碰不到清單。
  **仍然不採用，主要理由只有一條：前台的「點一下就存」在 Excel 上做不好。**
  清單的每一列有永久 ID，改一個欄位就是一次寫入；Excel 要先知道那一列在第幾列，
  而列號會因為排序或插入而變動，寫錯位置不會報錯。
  次要理由：Excel 無型別約束（三週後同一個狀態會有三種寫法）、
  稽核只到整份檔案而非單列、Claude 寫 Excel 是整份覆寫（有人開著就被蓋掉）、
  以及之後七個模組的讀寫邏輯要各寫一套，違反「殼裡只寫一次」。
  **採用的折衷：Excel 當進出口，清單當唯一資料源。**

- **不用 Power Apps。** 見第五節第 3 條。

- **Claude Code 暫不使用。** 判斷點不是檔案大小，是「能不能一次把整份檔案重寫交付」。
  走到只能給「第幾行改成什麼」就太晚。預估第三到第四個模組時導入。

- **Claude Cowork 不用來做系統**（地端個人工具無法多人共用），
  但適合一次性資料整理：Excel 轉匯入格式、舊資料清理、欄位盤點。

- **Vercel 值得做但先不動。** 價值在預覽環境（每次改動先有測試網址），
  那是目前缺的安全網。等公司 GitHub 組織的答案再決定，否則程式碼仍在個人 repo，
  治理問題只解一半。另需確認付費方案（Hobby 條款為非商業用途）。

---

## 六、貫穿全系統的三條規則

**一、一個欄位值得存，只有當它會被再次讀取。**
讀取次數 × 每次省下的時間，要大於輸入它的時間。
存：到期日、狀態、負責人、檔案連結。
不存：三家比價金額、工項細部金額、施工日誌。

**（2026-08-11 補）推論：不可能忘記的事不需要記。**
展覽前一天必然到現場布展，所以「到位」不設欄位——勾它不產生任何資訊。

**二、別的系統已經有的，只存指標。**
ERP 有金額 → 存編號或連結。EIP 有文件庫 → 存連結。公檔有檔案 → 存路徑。

**三、狀態要能點一下完成，不能要打字。**
任何需要打字的狀態欄位，三週後會是空的。

## 六之二、兩種「完成」訊號，何時用哪一種（2026-08-11 新增）

一件事做完了，系統怎麼知道？只有兩種來源，判準是**有沒有時間差**。

**用勾（人自己講）** — 當「做完」和「檔案歸檔」是兩件事、中間有時間差。
例：結案簡報寄給業務了，下週才想到要歸檔。這時系統去看資料夾會誤判成沒做完。

**用檔案存在（系統自己看）** — 當「檔案在不在」就是「做完沒做完」本身。
例：現場照片。把照片放進資料夾這個動作，就是整理照片，沒有時間差可言。
這時再多一個勾，等於要做兩次動作記錄同一件事，那個勾三週後會是空的。

**所以不對稱是刻意的，不是漏掉。** 展覽的展後階段：結案簡報用勾、現場照片用資料夾。

**代價要知道**：用資料夾當訊號，前台必須真的去讀資料夾內容（階段二）。
在那之前只有一顆「打開資料夾」的按鈕，完成狀態系統看不到。

**前台顯示檔案的兩階段（2026-08-11 新增）**：
- 階段一：用 `FolderKey` 組出固定路徑，只給「打開資料夾」的按鈕。
  純字串組合、不呼叫 Graph，寫錯最多是連結打不開，頁面不會壞。
- 階段二：呼叫 Graph 讀資料夾內容、列出實際檔案、照片出縮圖，完成狀態自動亮。

**不要一次做完。** 沒有預覽環境，正式版一 commit 全公司立刻看到；
階段一壞掉是按鈕沒反應，階段二壞掉可能整頁白掉。

---

## 七、命名與技術慣例

**欄位內部名稱一律英文，顯示名稱中文。**
中文欄位會被編碼成 `_x5c55__x89bd_` 這類亂碼，而 Graph 回傳的是內部名稱。
**內部名稱建立後無法修改，只能刪掉重建。** 這是整個專案唯一沒有第二次機會的地方。

**（2026-08-11 新增）「沒有第二次機會」的範圍要講清楚，不要嚇自己：**

| 項目 | 能不能改 |
|---|---|
| 欄位內部名稱 | **不能**，只能刪掉重建 |
| 清單的網址路徑 | **不能** |
| 網站的網址路徑 | **不能** |
| 欄位顯示名稱 | 隨時可以 |
| 選項欄位的選項值 | 可以增刪改 |
| 欄位型別 | 多數可以改（文字轉選項可以） |
| **新增欄位** | 隨時可以，對既有資料就是空白，不會壞任何東西 |

所以「先不加、以後要再加」是安全的決定。唯一的代價是**回填要靠記憶**，
所以刪掉的欄位值要先備份成 Excel。

**（2026-08-11 新增）日期欄位一律用「僅日期」（`Format="DateOnly"`），不要「日期及時間」。**
舊清單用了日期及時間，加上網站時區設定，畫面顯示比實際存的值早一天。
寫入時值用 `T12:00:00Z`（正午），這樣任何時區偏移都不會改變日期。

**清單命名**：英文、無空格，見第三節對照表。

**正式版就是 `index.html`（根目錄）。** 舊版一律移入 `archive/`，不留在根目錄。
理由：GitHub 每次 commit 就是版本控制，額外留一個「退路檔」是重複做同一件事，
還會製造「哪一個才是正式版」的混淆。

**重新導向 URI**：根目錄 `https://yulunyeh0731.github.io/` 已註冊在 Entra，
程式裡用單一 `REDIRECT_URI` 常數。只要正式版維持在根目錄，就不必再動 Entra。

**新增模組的步驟**（三步，每個新模組都一樣）：
1. 在「內部工具」網站建一張清單，欄位用英文內部名稱
2. 殼裡加一個選單項目
3. 加一段繪製程式，沿用 `FIELD_MAP` 的英文→中文轉換慣例

---

## 七之二、Claude 能碰到什麼（2026-08-11 新增）

這一節決定每次要請誰動手，不要每個對話重新試一遍。

**Microsoft 365 連接器（伺服器對伺服器，一直可用）**
- 能：搜尋全公司檔案、讀檔案內容（xlsx/pdf/pptx/docx）、建資料夾、上傳、覆寫、
  搬移、複製、改名、刪除（進資源回收筒）、讀信、寫草稿、讀 Teams 訊息、讀行事曆
- **不能：SharePoint 清單完全碰不到。** 沒有任何清單項目或欄位的工具。
  「檔案」和「清單」在 Graph 裡是不同資源。

**Claude in Chrome（要另外連上，見第十節）**
- 連上之後可以操作瀏覽器，包含在 SharePoint 頁面上直接呼叫 REST API。
  建清單、建欄位、改型別、寫資料、加成員、建資料夾都做得到，
  而且比用滑鼠點介面可靠——型別和內部名稱完全照規格，不靠猜。
- 這是目前唯一能讓 Claude 動到清單的路徑。

**永遠不代做的**：輸入密碼、輸入金融資訊。寄信、發訊息、送出表單、刪除，
每一次都要先問過。

---

## 八、尚未決定

- **第二個模組做哪個。** 年度行事曆最便宜（欄位少、只有本人看、拿來練
  「在殼裡加模組」）；受理登記回饋最多（兩位企劃同事會用）。
- **選單項目的名稱要對齊哪一邊。**（2026-08-11 新增）線上是「基礎資料」「回收桶」，
  本文件第三節是「分類設定」、無回收桶。
- **對外資料的出口清單。** 官網、公司 profile、工廠 profile、社群是否都算？
  工廠 profile 是一份還是每廠一份？還有沒有漏的（電子看板、型錄、投標文件）？
- **對外資料的類別清單。** 目前推測：認證獎項、ESG 慈善、環境碳數據、
  海外產區文件、公協會參與。待確認增刪。
- **儀表板的「已結案」要不要移除。** 按五之三該移，待確認。
- **年度行事曆的內容。** 要從使用者實際在追的工作問出來，
  不從任何既有文件推。她手上會有很多參考資料，那些是背景，不是規格。
- **受理登記的類別清單。** 可從 `sites/a_` 的
  `Shared Documents/General/職掌說明書/管理部_總務/` 推，不用從零盤點。
- **（2026-08-11 新增）展覽的補助要不要記死線。** 每個補助單位的時間點都不一樣，
  現在補助那組四欄沒有任何日期，系統無法提醒。目前決定不加，有問題再加。
- **（2026-08-11 新增）參展性質與執行單位要不要補回。** 舊清單有這兩欄，
  值分別是產品線（成衣／面料／成衣&面料）與執行者（企劃組執行／上海同仁自行參展）。
  這次刪掉，理由：同事填新表時參展性質一筆都沒填、執行單位 9 筆有 8 筆一樣。
  原值已備份在《舊Exhibitions清單_備份_13筆.xlsx》，要補時有依據。

---

## 九、建置順序

1. ~~補完展覽模組~~ → **改為：把前台接上重建後的清單**（2026-08-11 修正）
2. **給兩位企劃同事用兩三週**，收回饋修
3. **改成殼 ＋ 展覽模組**（已完成）
4. **年度行事曆**（第二個模組，練流程）
5. **受理登記**（第三個，有同事回饋）
6. **對外資料**（第四個，最複雜，那時已熟）
7. **專案、待辦、儀表板**

**不要同時開兩個工。** 使用者是兼著本職做的。

---

## 十、協作注意事項

（沿用 `交接說明_v2` 第七節，另補）

- **卯時設計案件管理系統（`maoshi-system.vercel.app/history`）只能提供形狀，
  不能提供結論。** 每個具體建議都要能從使用者自己的事實單獨推出來。
  已犯過的錯：把簽呈綁專案（她有 ERP 和 EIP）、建議展覽系統過渡不搬
  （她早說是做一半的）、建議權限矩陣（她只有 7 個人）、建議 Power Apps
  （她已完成 HTML 且 Claude 無法交付 Power App）。
- 她會明確指出前後矛盾與跑偏，**她的糾正通常是對的**。
- 偏好一次問一類問題。
- 不熟悉術語，會直接說看不懂。不要給她看不懂的話讓她拿去跟 IT 講。

**（2026-08-11 新增）以下四條都是這次實際踩到的：**

- **範例值會被當成規格。** 交給同事填的表裡如果放了猜的範例值，那個猜測會被當真。
  這次在填寫表的「參展性質」欄放了瞎猜的「貿協團」，差點讓一個裝著產品線資訊的
  欄位被誤刪。**範例值要嘛用真實資料，要嘛留空。**

- **不要主動撈出使用者沒提過的舊檔案來當依據。** 這次搜到一份放在別人 Teams
  聊天附件裡、快一年沒動的年度參展表，然後端到她面前要她判斷。那跟「把前任交接
  清單當成她的工作清單」是同一個動作。她的原則是**從乾淨的地方開始**。

- **Claude in Chrome 必須在每一個對話裡各自啟用。** 這不是安裝問題、不是帳號問題、
  不是網路問題。桌面版設定裡的 Enabled 只代表功能可用；每個新對話要在該對話的
  Connectors 控制項裡再打開一次。這次為此排查了很久，方向全錯。

- **刪除前先備份成 Excel，不要相信「內容不準確所以可以直接刪」。**
  這次舊清單裡有四樣新表沒有的東西（兩個欄位的值、三個連結、六場評估中／暫不參加
  的紀錄），其中兩個欄位當下看起來是廢欄位，實際上裝著有意義的資訊。

## 收場必做

每個對話結束前交付：

1. 更新後的 `DEVLOG.md`（做了什麼、為什麼那樣決定、踩到什麼坑）
2. 新版 `index.html`（如果有改）
3. **commit 訊息**（標題一行 ＋ 條列說明）
4. 骨幹規劃如果有定案變動，一併更新
